{
  "name": "sgs-relay-server",
  "version": "1.0.0",
  "description": "Smart Grid Sentinel — Local relay server. Bridges ESP32 HTTP/MQTT to browser WebSocket.",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "mqtt": "^5.3.4",
    "ws": "^8.17.0"
  }
}
